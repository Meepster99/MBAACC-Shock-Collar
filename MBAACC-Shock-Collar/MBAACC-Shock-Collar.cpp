
#include <stdio.h>
#include <windows.h>
#include <iostream>
#include <string>
#include <iostream>

// make sure to run vcpkg install --triplet x86-windows
#include <curl/curl.h>

static_assert(sizeof(void*) == 4, "program must be compiled in 32 bit mode");

void sendShock() {



}

int main() {

	printf("omfg\n");
 
    while (true) {

       
        Sleep(100);
    };


	return 0;
}

